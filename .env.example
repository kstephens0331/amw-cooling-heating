# AMW Cooling & Heating — Frontend Environment Variables
# Copy this file to .env.local and fill in real values. Never commit .env.local.

# Not required for production — chatbot runs through backend proxy
# PORT=3000
